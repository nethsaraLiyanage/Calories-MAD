<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mad_y2s2">

    <application
        android:allowBackup="true"
        android:icon="@drawable/calories"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
      
        <activity android:name=".exercise"></activity>
        <activity android:name=".activity_calorieStatus"></activity>
        <activity android:name=".MainActivity" ></activity>
        <activity android:name=".activity_home"></activity>
        <activity android:name=".LoginUsers" ></activity>
        <activity android:name=".SignupActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity_statistics"></activity>
        <activity android:name=".activity_total_food_calories_count"></activity>
        <activity android:name=".AddMeal"></activity>
        <activity android:name=".Update_meal"></activity>
        <activity android:name=".UpdateActivity"></activity>
        <activity android:name=".Bmi">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>